services:
  mysqldb_send:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: senha
      MYSQL_DATABASE: public
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always
    ports:
      - "3301:3306"

volumes:
  mysql_data:
